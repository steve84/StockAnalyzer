import { DialogsizeDirective } from './dialogsize.directive';

describe('DialogsizeDirective', () => {
  it('should create an instance', () => {
    const directive = new DialogsizeDirective();
    expect(directive).toBeTruthy();
  });
});
