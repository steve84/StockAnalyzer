import { StockcategoryPipe } from './stockcategory.pipe';

describe('StockcategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new StockcategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
